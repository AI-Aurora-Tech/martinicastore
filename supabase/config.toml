# Configuração das Edge Functions (usado pela Supabase CLI).
# O webhook do Mercado Pago é chamado pelo MP sem JWT — precisa ser público.
[functions.mercadopago-webhook]
verify_jwt = false

# A criação de pedido é chamada pelo navegador do cliente (anon) — sem JWT.
[functions.place-order]
verify_jwt = false
